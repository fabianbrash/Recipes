```Ubuntu Cloud Image info```


#### The default username is ubuntu, during setup of the OVA you MUST enter an SSH public key and you MUST log into the system for the first time with SSH so you can change the password, my thinking is that they really want you to setup user-data
